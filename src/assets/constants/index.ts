export enum ROUTE_PATH {
    HOME = '/',
    ABOUT = '/about',
    PRICE = '/price',
    DELIVERY = '/delivery',
    CONTACTS = '/contacts'
}

export enum APP_THEME {
    DARK = 'dark',
    LIGHT = 'light'
}
