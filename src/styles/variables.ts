import {css} from 'styled-components'

const variables = css`
  :root {
    // Colors
    --white: #fff;
    --black: #000;
    --gray: #7f7f7f;
    --blue: #057dcd;

    // Other
    --transition: all 0.25s ease-in-out;
  }
`

export default variables
