import React from 'react'
import { createRoute } from 'atomic-router'

export const deliveryRoute = createRoute()

export const Delivery: React.FunctionComponent = () => {
    return <div>Delivery</div>
}
